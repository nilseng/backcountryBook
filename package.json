{
  "name": "back-country-book",
  "version": "1.0.0",
  "description": "Web app for sharing back country skiing experiences",
  "main": "server/src/index.ts",
  "scripts": {
    "heroku-prebuild": "npm i && cd client && npm i && npm run build && cd .. && cd server && npm i && tsc"
  },
  "author": "Teodor Nilseng Danielsen",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.1.3"
  }
}
